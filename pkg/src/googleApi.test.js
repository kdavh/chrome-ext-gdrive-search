import { expect } from 'chai'

describe('googleApi', function() {
  describe('#getFileList()', function() {
    it('should return promise that resolves to array of files', function() {
      expect(2).to.equal(2)
    });
  });
});
